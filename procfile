web: python app.py
release: python -c "
import os, time
from app import ensure_postgres_schema, build_drug_features
print('🚀 Starting pre-deployment setup...')
try:
    ensure_postgres_schema()
    print('✅ Database schema initialized')
    df = build_drug_features(limit=50)
    print(f'✅ AI model prepared with {len(df)} drugs')
except Exception as e:
    print(f'⚠️ Setup completed with notes: {e}')
print('🎉 Pre-deployment completed!')
"